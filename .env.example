# Copy to .env and adjust. Do not commit .env (gitignored).
# EXPO_PUBLIC_* is visible in the client bundle — use only for non-secret config.

# Backend origin (no trailing slash). Default in code if unset: http://localhost:8000
EXPO_PUBLIC_API_URL=http://localhost:8000
